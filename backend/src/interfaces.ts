export interface LoginBody {
    username  : string;
    password  : string;
    _id       : string;
    code      : string;
  }
  
  // 注册接口类型
  export interface SignupBody {
    username  : string;
    password  : string;
    _id       : string;
    code      : string;
  }