export interface LoginBody {
    username  : string;
    password  : string;
    _id       : string;
    code      : string;
  }
  
  export interface SignupBody {
    username  : string;
    password  : string;
    _id       : string;
    code      : string;
  }